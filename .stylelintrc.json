{
  "$schema": "https://schema.easynm.cn/stylelintrc.json",
  "extends": ["@dcm/stylelint-config"],
  "rules": {
    "plugin/no-unsupported-browser-features": [
      true,
      {
        "severity": "warning",
        "ignore": ["user-select-none"]
      }
    ]
  }
}
